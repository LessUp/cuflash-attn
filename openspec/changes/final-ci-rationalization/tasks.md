# Tasks: final-ci-rationalization

- [ ] 修复 `release.yml` 的 Node 环境与 docs 构建前置
- [ ] 修复 `codeql.yml` 对 preset-only 构建规则的偏离
- [ ] 审核 `ci.yml` / `pages.yml` 的触发条件与重复检查
- [ ] 保持无 GPU runner 下的 CI 行为清晰、可解释、维护成本低
